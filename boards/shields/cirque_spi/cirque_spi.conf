# Cirque Pinnacle trackpad over SPI
CONFIG_SPI=y
CONFIG_ZMK_POINTING=y

# Let the trackpad follow ZMK's idle state instead of sleeping on its own.
# Leave off unless you have verified wake-from-sleep works on your unit.
#CONFIG_ZMK_INPUT_PINNACLE_IDLE_SLEEPER=y
